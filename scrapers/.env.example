# F-List Scraper Configuration
# Copy this file to .env and fill in your credentials
# NEVER commit .env to version control

# F-List Account Credentials
# Required for API access (character data, kink lists, etc.)
# Create an account at https://www.f-list.net if you don't have one
FLIST_ACCOUNT=your_flist_username
FLIST_PASSWORD=your_flist_password

# Optional: Override default storage location
# FLIST_STORAGE_ROOT=/Volumes/David External/flist_archive

# Optional: Override default rate limits (in seconds)
# FLIST_RATE_LIMIT_API=1.0
# FLIST_RATE_LIMIT_SCRAPE=2.0
